on:
  schedule:
    - cron: '*/1 * * * *' # Attempt to run every minute
  workflow_dispatch:      # Keep this so you can still click the button

jobs:
  test-cron:
    runs-on: ubuntu-latest
    steps:
      - name: Heartbeat
        run: |
          echo "The Moss Bot is awake!"
          echo "Current time: $(date)"
